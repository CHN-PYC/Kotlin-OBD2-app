# RAG Vehicle QA 改造方案

## 目标

把现有 OBD2 诊断 App 从“规则诊断 + 远程 LLM 诊断”扩展为“基于车辆数据的 RAG 问答系统”。

用户可以针对某次行程直接提问，例如：
- 为什么这次冷却液温度偏高？
- 这个 STFT / LTFT 组合更像偏稀还是偏浓？
- 电瓶电压正常吗？

系统会：
1. 读取该行程的车辆数据与规则诊断摘要
2. 分析用户问题，匹配最相关的车辆诊断资料源
3. 自动下载并缓存外部文档作为知识库片段
4. 做本地 chunk + 关键词检索
5. 将车辆数据 + 检索片段一起发送给远程 LLM
6. 返回带依据的车辆问答结果

## 第一阶段（本次已实现）

- 增加 `RAG_QA` 报告类型
- 增加知识源目录 `VehicleQaKnowledgeCatalog`
- 增加知识文档下载与缓存 `KnowledgeDocumentDownloader`
- 增加本地片段切分与检索 `KnowledgeSnippetRetriever`
- 增加 RAG 问答编排服务 `VehicleQaRagService`
- 在 History 页面加入 `Ask Vehicle QA (RAG)` 入口
- 支持把用户问题、知识源、检索片段、车辆摘要打包给远程 LLM

## 当前实现边界

- 文档筛选目前使用关键词匹配，不是嵌入召回
- 知识库来源是预置的外部资料列表
- 远程 LLM 仍然是唯一生成端，本地只做检索和拼接
- 未做复杂 HTML 正文抽取，只做轻量清洗

## 后续建议

### 第二阶段
- 引入向量化检索（本地或服务端）
- 按 PID / 故障主题建立更细粒度知识索引
- 增加知识源管理页，允许增删资料源
- 把知识库缓存状态展示到 UI

### 第三阶段
- 将 OBD 实时数据、历史 session、规则诊断、RAG 文档统一到一个 Agent 工作流
- 支持多轮追问
- 支持回答中的来源引用跳转
- 加入问答日志与用户反馈闭环
